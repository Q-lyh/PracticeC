# ========================== VS2022 C项目 核心忽略规则 ==========================
# 1. VS2022工作区缓存（解决你遇到的.vs/权限问题）
.vs/
# 2. 用户级配置（本地调试/路径/布局，仅自己电脑有效）
*.suo                # 解决方案用户选项
*.user               # 通用用户配置
*.vcxproj.user       # C项目专属用户配置（调试参数、环境变量等）
*.sln.docstates      # 解决方案文档状态
# 3. 编译产物（每次构建都会重新生成，无需上传）
[Dd]ebug/            # Debug模式编译输出
[Rr]elease/          # Release模式编译输出
x64/                 # 64位编译目录
x86/                 # 32位编译目录
[Bb]in/              # 可执行文件输出
[Oo]bj/              # 目标文件（.obj）目录
# 4. 编译临时文件/日志
*.log                # 构建日志
*.tlog               # 跟踪日志
*.lastbuildstate     # 最后构建状态文件
*.Build.CppClean.log # 清理构建日志
# 5. 调试/符号文件
*.pdb                # 调试符号文件
*.idb                # 增量编译数据库
*.ilk                # 链接器增量库
# 6. 可执行文件（C项目编译产物）
*.exe                # 最终生成的exe程序
*.dll                # 若有动态库则忽略
*.lib                # 若有静态库则忽略
# ========================== 系统/编辑器通用忽略 ==========================
# Windows系统文件
Thumbs.db
Desktop.ini
$RECYCLE.BIN/
# 编辑器缓存（若用VS Code/Notepad++等辅助编辑）
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
# 临时文件
*.tmp
*.bak
*.swp